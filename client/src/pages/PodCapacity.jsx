import Placeholder from './Placeholder.jsx';

export default function PodCapacity() {
  return (
    <Placeholder
      title="Pod Capacity"
      blurb="Real dev-board/Jira status per pod (Clinical, CRM, etc.) instead of self-reported workload — the gap Tyler and William were fighting over."
      status="planned"
      note="Also needs the Jira/Atlassian connector. Build after Request Queue."
    />
  );
}
