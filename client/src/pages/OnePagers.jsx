import Placeholder from './Placeholder.jsx';

export default function OnePagers() {
  return (
    <Placeholder
      title="One-Pagers"
      blurb="Shareable, exportable brief per issue/account — column/row descriptions, data source, current state — so a decision-maker can review async before a meeting instead of live in it. John's literal ask."
      status="planned"
      note="Depends on Request Queue and Account Truth existing first, since a one-pager pulls from both."
    />
  );
}
