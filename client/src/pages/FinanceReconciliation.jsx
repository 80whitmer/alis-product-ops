import Placeholder from './Placeholder.jsx';

export default function FinanceReconciliation() {
  return (
    <Placeholder
      title="Finance Reconciliation"
      blurb="Dave's view: enabled-but-uncontracted flags, MRR blocked in billing status, and ARR-weighted exposure on open high-priority requests. Same data spine as the rest of this app, kept on its own surface — Dave shouldn't have to look at product-prioritization noise to get his answer."
      status="planned"
      note="Depends on the Request Queue's data spine (#1) for the ARR-exposure piece."
    />
  );
}
